name: Model Retraining Workflow

on:
  schedule:
    # Run weekly at midnight on Sunday
    - cron: '0 0 * * 0'
  workflow_dispatch: # Allow running manually from the GitHub UI

jobs:
  retrain:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Retraining Script
      run: |
        python scripts/retrain.py
        
    # Optional: You could add a step here to commit the new model
    # back to the repository, or upload it to S3, etc.
    # - name: Commit new model
    #   run: |
    #     git config --global user.name "github-actions[bot]"
    #     git config --global user.email "github-actions[bot]@users.noreply.github.com"
    #     git add model/
    #     git commit -m "Update retrained model automatically"
    #     git push
