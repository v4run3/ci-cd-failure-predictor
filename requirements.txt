# requirements.txt - Project Dependencies
#
# Purpose:
#   List all Python package dependencies required for the
#   CI/CD Failure Predictor project.
#
# Future additions:
#   - pandas (data manipulation)
#   - scikit-learn (model training and evaluation)
#   - fastapi (API framework)
#   - uvicorn (ASGI server)
#   - requests (HTTP client for log fetching)
#   - prometheus-client (metrics export)
#   - joblib (model serialization)
#   - pytest (testing framework)

# Phase 1: Data generation and ingestion
pandas>=2.0.0
Faker>=18.0.0

# Phase 3: Model training, evaluation, and prediction
scikit-learn>=1.3.0
joblib>=1.3.0

# Phase 4: REST API service
fastapi>=0.100.0
uvicorn>=0.23.0

# Testing (required by FastAPI TestClient)
httpx>=0.24.0
pytest>=7.0.0
